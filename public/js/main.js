/* let colorMenu = document.querySelectorAll('.side-menu'); 
document.addEventListener("DOMContentLoaded", color);



function color (){
     
    colorMenu.forEach(e => {
         console.log("Hola");
        if(e.getAttribute("href") == "/rol"){
            if(e.classList.contains('side-menu--active')){
                e.classList.remove('side-menu--active');
            }
            else {
                e.classList.add('side-menu--active');
                console.log("No existe");
            }
        }
        e.addEventListener('click', () => {
        
           
            
        });
    })
    
} */


 